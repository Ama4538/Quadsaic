const Loader = () => {
    return (
        // Loader for the page refresh when data is being loaded
        <section className="loader">
            <div className="loader__animation"><div></div><div></div><div></div></div>
        </section>
    )
}

export default Loader